name: enif
description: Automate business operations, boost business growth and increase customer satisfaction with streamlined automation.
version: 0.1.2
homepage: https://github.com/crednet/enif_sdk.git

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  data_repository: ^0.4.1
  flutter_animate: ^4.2.0+1
  flutter_svg: ^2.0.7
  # image_picker: ^0.8.4+4
  shared_preferences: ^2.2.1
  flutter_html: ^3.0.0-beta.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  assets:
  - assets/images/
  - assets/svg/
